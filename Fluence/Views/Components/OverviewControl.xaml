<UserControl
    x:Class="Fluence.Views.Components.OverviewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components"
    xmlns:converters="using:Fluence.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="360">

    <UserControl.Resources>
        <converters:WidthConverter x:Key="WidthConverter" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="0,0,10,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Height="30" Background="#1AFFFFFF" Margin="0,0,0,5">
                <Grid>
                    <Rectangle Fill="{Binding BudgetBrush}" HorizontalAlignment="Left">
                        <Rectangle.Width>
                            <Binding Path="BudgetPercent">
                                <Binding.Converter>
                                    <StaticResource ResourceKey="WidthConverter" />
                                </Binding.Converter>
                                <Binding.ConverterParameter>340</Binding.ConverterParameter>
                            </Binding>
                        </Rectangle.Width>
                    </Rectangle>
                </Grid>
            </Border>
            <TextBlock Grid.Row="1" Text="monthly budget health" FontSize="20" Margin="0,0,0,20" Foreground="{ThemeResource SystemColorControlAccentBrush}" />
            
            <!-- Advanced Hybrid Insights -->
            <StackPanel Grid.Row="2" Margin="0,0,0,20">
                <TextBlock Text="{Binding RunwayDays}" FontSize="36" FontWeight="Bold" />
                <TextBlock Text="estimated days left until balance reaches zero" FontSize="14" Opacity="0.6" Margin="0,2,0,0" />
                <TextBlock Text="{Binding SavingOpportunitiesSummary}" FontSize="12" Foreground="Gray" Margin="0,5,0,5" />
                <TextBlock Text="financial runway" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,0,0,0" />
            </StackPanel>

            <TextBlock Grid.Row="3" Text="{Binding CurrentBalance, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Grid.Row="4" Text="current balance" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,20" />

            <TextBlock Grid.Row="5" Text="{Binding DailyAllowance, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Grid.Row="6" Text="daily allowance" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />

            <TextBlock Grid.Row="7" Text="{Binding SpentToday, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Grid.Row="8" Text="spent today" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />

            <TextBlock Grid.Row="9" Text="{Binding WeeklyTotal, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Grid.Row="10" Text="weekly total" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />

            <TextBlock Grid.Row="11" Text="{Binding TopCategory}" FontSize="36" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
            <TextBlock Grid.Row="12" Text="top category" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />

            <TextBlock Grid.Row="13" Text="{Binding DaysUntilPayday}" FontSize="36" FontWeight="Bold" />
            <TextBlock Grid.Row="14" Text="days until payday" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />
        </Grid>
    </ScrollViewer>
</UserControl>
