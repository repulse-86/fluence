<Page
    x:Class="Fluence.Views.TransactionDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PageTitle"
                   Text="{Binding CategoryName}"
                   Style="{ThemeResource HeaderTextBlockStyle}"
                   Margin="19,0,-100,26"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"
                   TextWrapping="NoWrap" />

        <ScrollViewer Grid.Row="1" Margin="19,0,19,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Foreground="{ThemeResource SystemColorControlAccentBrush}" Margin="0,0,0,30" IsHitTestVisible="False">
                    <Run Text="{Binding DisplayDate}" FontSize="28" />
                    <Run Text="  " FontSize="28" />
                    <Run Text="{Binding DisplayTime}" FontSize="28" />
                </TextBlock>

                <TextBlock Grid.Row="1" Text="{Binding DisplayAmount}"
                           Foreground="{Binding TypeBrush}"
                           FontSize="84"
                           FontWeight="Bold"
                           Margin="0,0,0,5" IsHitTestVisible="False" />

                <TextBlock Grid.Row="2" Text="{Binding Type}"
                           FontSize="24"
                           Foreground="{Binding TypeBrush}"
                           Opacity="0.8"
                           Margin="0,0,0,30" IsHitTestVisible="False" />

                <TextBlock Grid.Row="3" Text="{Binding Note}"
                           FontSize="24"
                           TextWrapping="Wrap"
                           Margin="0,0,0,30" IsHitTestVisible="False" />
            </Grid>
        </ScrollViewer>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="EditAppBarButton" Icon="Edit" Label="edit" Click="EditAppBarButton_Click" />
            <AppBarButton x:Name="DeleteAppBarButton" Icon="Delete" Label="delete" Click="DeleteAppBarButton_Click" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
