<UserControl
    x:Class="Fluence.Views.Components.HistoryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="360"
    Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <CollectionViewSource x:Name="GroupedTransactionsSource" 
                              Source="{Binding GroupedTransactions}" 
                              IsSourceGrouped="True" />
    </UserControl.Resources>

    <Grid Margin="0,-8,0,0">
        <ListView x:Name="TransactionList"
                    ItemsSource="{Binding Source={StaticResource GroupedTransactionsSource}}"
                    HorizontalContentAlignment="Stretch"
                    IsItemClickEnabled="True"
                    ItemClick="TransactionList_ItemClick">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,5" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border Background="Transparent" Margin="0,0,0,10">
                                <TextBlock Text="{Binding DateKey}" FontSize="20" Foreground="{ThemeResource SystemColorControlAccentBrush}" />
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,20" Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding DisplayTime}" FontSize="14" Margin="0,0,0,4" IsHitTestVisible="False" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding CategoryName}" FontSize="24" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" IsHitTestVisible="False" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DisplayAmount}" Foreground="{Binding TypeBrush}" FontSize="20" FontWeight="Thin" VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
