<UserControl
    x:Class="Fluence.Views.Components.ReportsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components"
    xmlns:converters="using:Fluence.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="360">

    <UserControl.Resources>
        <converters:WidthConverter x:Key="WidthConverter" />
    </UserControl.Resources>

    <Grid Margin="-16,-44,0,0">
        <Pivot x:Name="ReportsPivot">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="24" FontWeight="SemiBold" />
                </DataTemplate>
            </Pivot.HeaderTemplate>

            <PivotItem Header="day" DataContext="{Binding DayReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <StackPanel>
                        <TextBlock Text="distribution" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding Distribution}" Margin="0,0,0,30">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Background="#33FFFFFF" Height="30" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Fill="{Binding Color}" Width="{Binding Percentage, Converter={StaticResource WidthConverter}, ConverterParameter=340}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="comparison" FontSize="18" Margin="0,0,0,10" />
                        <Grid Margin="0,0,0,30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100" />
                                <RowDefinition Height="100" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Background="#33FFFFFF" Margin="0,0,5,5" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="savings rate" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding SavingsRate}" Foreground="#2ECC71" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White">
                                        <Run Text="{Binding SavingsRate, ConverterParameter='N2'}" FontSize="24" />
                                        <Run Text="%" FontSize="14" />
                                    </TextBlock>
                                </Grid>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Background="#33FFFFFF" Margin="5,0,0,5" Padding="15">
                                <Grid>
                                    <TextBlock Text="vs yesterday" FontSize="12" Opacity="0.8" VerticalAlignment="Top" Foreground="White" />
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding TrendDirection}" FontSize="18" Margin="0,0,5,0" VerticalAlignment="Bottom" Foreground="White" />
                                        <TextBlock FontWeight="Bold" VerticalAlignment="Bottom" Foreground="White">
                                            <Run Text="{Binding TrendPercent, ConverterParameter='N2'}" FontSize="24" />
                                            <Run Text="%" FontSize="14" />
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" Background="#33FFFFFF" Margin="0,5,5,0" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="flow in" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding FlowInPercent}" Foreground="#2ECC71" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding FlowIn, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1" Background="#33FFFFFF" Margin="5,5,0,0" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="flow out" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding FlowOutPercent}" Foreground="#E74C3C" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding FlowOut, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White" />
                                </Grid>
                            </Border>
                        </Grid>

                        <TextBlock Text="top budget burners" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding BudgetBurners}" Margin="0,0,0,30">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0,0,0">
                                            <TextBlock Text="+" FontSize="14" Margin="0,2,2,0" />
                                            <TextBlock Text="{Binding Trend, ConverterParameter='N2'}" FontSize="18" FontWeight="Bold" Foreground="#E74C3C" />
                                            <TextBlock Text="%" FontSize="12" Margin="2,5,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="breakdown" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding Distribution}" Margin="0,0,0,30">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,15">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" FontSize="14" TextTrimming="CharacterEllipsis" MaxWidth="200" />
                                            <TextBlock Text="{Binding TransactionCount}" FontSize="12" Margin="10,3,0,0" />
                                        </StackPanel>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Amount, ConverterParameter='N2'}" FontSize="16" FontWeight="SemiBold" />
                                        <ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Value="{Binding Percentage}" Foreground="{Binding Color}" Height="4" Margin="0,5,0,0" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="month" DataContext="{Binding MonthReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="distribution" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding Distribution}" Margin="0,0,0,30">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Background="#33FFFFFF" Height="30" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Fill="{Binding Color}" Width="{Binding Percentage, Converter={StaticResource WidthConverter}, ConverterParameter=340}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="spending intensity" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding IntensityMap}" Margin="0,0,0,30">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapGrid Orientation="Horizontal" MaximumRowsOrColumns="16" VerticalChildrenAlignment="Top" HorizontalChildrenAlignment="Left" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="18" Height="18" Background="#2ECC71" Opacity="{Binding Intensity}" Margin="1" CornerRadius="2" ToolTipService.ToolTip="{Binding Label}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="comparison" FontSize="18" Margin="0,0,0,10" />
                        <Grid Margin="0,0,0,30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100" />
                                <RowDefinition Height="100" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Background="#33FFFFFF" Margin="0,0,5,5" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="savings rate" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding SavingsRate}" Foreground="#2ECC71" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White">
                            <Run Text="{Binding SavingsRate, ConverterParameter='N2'}" FontSize="24" />
                            <Run Text="%" FontSize="14" />
                                    </TextBlock>
                                </Grid>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Background="#33FFFFFF" Margin="5,0,0,5" Padding="15">
                                <Grid>
                                    <TextBlock Text="vs last month" FontSize="12" Opacity="0.8" VerticalAlignment="Top" Foreground="White" />
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding TrendDirection}" FontSize="18" Margin="0,0,5,0" VerticalAlignment="Bottom" Foreground="White" />
                                        <TextBlock FontWeight="Bold" VerticalAlignment="Bottom" Foreground="White">
                                <Run Text="{Binding TrendPercent, ConverterParameter='N2'}" FontSize="24" />
                                <Run Text="%" FontSize="14" />
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" Background="#33FFFFFF" Margin="0,5,5,0" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="flow in" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding FlowInPercent}" Foreground="#2ECC71" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding FlowIn, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1" Background="#33FFFFFF" Margin="5,5,0,0" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="flow out" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding FlowOutPercent}" Foreground="#E74C3C" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding FlowOut, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White" />
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid Margin="0,0,0,30">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock Text="daily average" FontSize="12" />
                                <TextBlock Text="{Binding DailyAverage, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock Text="projected total" FontSize="12" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding ProjectedTotal, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Right" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="top budget burners" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding BudgetBurners}" Margin="0,0,0,30">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0,0,0">
                                            <TextBlock Text="+" FontSize="14" Margin="0,2,2,0" />
                                            <TextBlock Text="{Binding Trend, ConverterParameter='N2'}" FontSize="18" FontWeight="Bold" Foreground="#E74C3C" />
                                            <TextBlock Text="%" FontSize="12" Margin="2,5,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="breakdown" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding Distribution}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,15">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" FontSize="14" TextTrimming="CharacterEllipsis" MaxWidth="200" />
                                            <TextBlock Text="{Binding TransactionCount}" FontSize="12" Margin="10,3,0,0" />
                                        </StackPanel>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Amount, ConverterParameter='N2'}" FontSize="16" FontWeight="SemiBold" />
                                        <ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Value="{Binding Percentage}" Foreground="{Binding Color}" Height="4" Margin="0,5,0,0" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="year" DataContext="{Binding YearReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="distribution" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding Distribution}" Margin="0,0,0,30">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Background="#33FFFFFF" Height="30" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Fill="{Binding Color}" Width="{Binding Percentage, Converter={StaticResource WidthConverter}, ConverterParameter=340}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="monthly intensity" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding IntensityMap}" Margin="0,0,0,30">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="2,0">
                                        <Border Width="24" Height="24" Background="#2ECC71" Opacity="{Binding Intensity}" CornerRadius="4" />
                                        <TextBlock Text="{Binding Label}" FontSize="10" HorizontalAlignment="Center" Margin="0,2,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="comparison" FontSize="18" Margin="0,0,0,10" />
                        <Grid Margin="0,0,0,30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100" />
                                <RowDefinition Height="100" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Background="#33FFFFFF" Margin="0,0,5,5" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="savings rate" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding SavingsRate}" Foreground="#2ECC71" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White">
                            <Run Text="{Binding SavingsRate, ConverterParameter='N2'}" FontSize="24" />
                            <Run Text="%" FontSize="14" />
                                    </TextBlock>
                                </Grid>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Background="#33FFFFFF" Margin="5,0,0,5" Padding="15">
                                <Grid>
                                    <TextBlock Text="vs last year" FontSize="12" Opacity="0.8" VerticalAlignment="Top" Foreground="White" />
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding TrendDirection}" FontSize="18" Margin="0,0,5,0" VerticalAlignment="Bottom" Foreground="White" />
                                        <TextBlock FontWeight="Bold" VerticalAlignment="Bottom" Foreground="White">
                                <Run Text="{Binding TrendPercent, ConverterParameter='N2'}" FontSize="24" />
                                <Run Text="%" FontSize="14" />
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" Background="#33FFFFFF" Margin="0,5,5,0" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="flow in" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding FlowInPercent}" Foreground="#2ECC71" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding FlowIn, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1" Background="#33FFFFFF" Margin="5,5,0,0" Padding="15">
                                <Grid>
                                    <StackPanel VerticalAlignment="Top">
                                        <TextBlock Text="flow out" FontSize="12" Opacity="0.8" Foreground="White" />
                                        <ProgressBar Value="{Binding FlowOutPercent}" Foreground="#E74C3C" Height="4" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding FlowOut, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="White" />
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid Margin="0,0,0,30">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock Text="monthly average" FontSize="12" />
                                <TextBlock Text="{Binding DailyAverage, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock Text="projected year" FontSize="12" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding ProjectedTotal, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Right" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="top budget burners" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding BudgetBurners}" Margin="0,0,0,30">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0,0,0">
                                            <TextBlock Text="+" FontSize="14" Margin="0,2,2,0" />
                                            <TextBlock Text="{Binding Trend, ConverterParameter='N2'}" FontSize="18" FontWeight="Bold" Foreground="#E74C3C" />
                                            <TextBlock Text="%" FontSize="12" Margin="2,5,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="breakdown" FontSize="18" Margin="0,0,0,10" />
                        <ItemsControl ItemsSource="{Binding Distribution}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,15">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" FontSize="14" TextTrimming="CharacterEllipsis" MaxWidth="200" />
                                            <TextBlock Text="{Binding TransactionCount}" FontSize="12" Margin="10,3,0,0" />
                                        </StackPanel>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Amount, ConverterParameter='N2'}" FontSize="16" FontWeight="SemiBold" />
                                        <ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Value="{Binding Percentage}" Foreground="{Binding Color}" Height="4" Margin="0,5,0,0" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
        </Pivot>
    </Grid>
</UserControl>