<Page
    x:Class="Fluence.Views.CategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="SaveCategoryButton" Icon="Save" Label="save" Click="SaveButton_Click"/>
            <AppBarButton x:Name="CancelCategoryButton" Icon="Cancel" Label="cancel" Click="CancelButton_Click"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Margin="19,2,19,0">
        <StackPanel>
            <TextBlock Text="manage category" Style="{ThemeResource HeaderTextBlockStyle}" Margin="0,0,0,20" />
            
            <TextBlock Text="category name" Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,5" />
            <TextBox Text="{Binding CategoryName, Mode=TwoWay}" PlaceholderText="e.g. Transportation" />
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
