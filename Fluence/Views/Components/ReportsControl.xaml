<UserControl
    x:Class="Fluence.Views.Components.ReportsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components"
    xmlns:converters="using:Fluence.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="360">

    <Grid Margin="-19,-44,0,0">
        <Pivot x:Name="ReportsPivot">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="24" FontWeight="SemiBold" />
                </DataTemplate>
            </Pivot.HeaderTemplate>

            <PivotItem Header="today" DataContext="{Binding DayReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <Grid Margin="0,0,19,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <local:ReportComparisonControl Grid.Row="0" Header="comparison" TrendText="vs yesterday" />
                        <local:ReportBurnersControl Grid.Row="1" Header="top budget burners" DataContext="{Binding BudgetBurners}" Margin="0,20,0,10" />
                        <Button Grid.Row="2" Content="show more" HorizontalAlignment="Stretch" Margin="0,20,0,20" Click="ShowMore_Click" Tag="today" />
                    </Grid>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="month" DataContext="{Binding MonthReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <Grid Margin="0,0,19,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <local:ReportComparisonControl Grid.Row="0" Header="comparison" TrendText="vs last month" />
                        <local:ReportBurnersControl Grid.Row="1" Header="top budget burners" DataContext="{Binding BudgetBurners}" />
                        <Button Grid.Row="2" Content="show more" HorizontalAlignment="Stretch" Margin="0,20,0,20" Click="ShowMore_Click" Tag="month" />
                    </Grid>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="year" DataContext="{Binding YearReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <Grid Margin="0,0,19,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <local:ReportComparisonControl Grid.Row="0" Header="comparison" TrendText="vs last year" />
                        <local:ReportBurnersControl Grid.Row="1" Header="top budget burners" DataContext="{Binding BudgetBurners}" />
                        <Button Grid.Row="2" Content="show more" HorizontalAlignment="Stretch" Margin="0,20,0,20" Click="ShowMore_Click" Tag="year" />
                    </Grid>
                </ScrollViewer>
            </PivotItem>
        </Pivot>
    </Grid>
</UserControl>
