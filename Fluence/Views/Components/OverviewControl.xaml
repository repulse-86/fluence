<UserControl
    x:Class="Fluence.Views.Components.OverviewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components"
    xmlns:converters="using:Fluence.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="360">

    <UserControl.Resources>
        <converters:WidthConverter x:Key="WidthConverter" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,16">

            <Border Height="30" Background="#1AFFFFFF" Margin="0,0,0,5">
                <Grid>
                    <Rectangle Fill="{Binding BudgetBrush}" HorizontalAlignment="Left">
                        <Rectangle.Width>
                            <Binding Path="BudgetPercent">
                                <Binding.Converter>
                                    <StaticResource ResourceKey="WidthConverter" />
                                </Binding.Converter>
                                <Binding.ConverterParameter>340</Binding.ConverterParameter>
                            </Binding>
                        </Rectangle.Width>
                    </Rectangle>
                </Grid>
            </Border>
            <TextBlock Text="monthly budget health" FontSize="20" Opacity="0.6" Margin="0,0,0,20" />
            
            <TextBlock Text="{Binding CurrentBalance, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Text="current balance" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,20" />

            <TextBlock Text="{Binding DailyAllowance, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Text="daily allowance" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />

            <TextBlock Text="{Binding SpentToday, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Text="spent today" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />

            <TextBlock Text="{Binding WeeklyTotal, ConverterParameter='N2'}" FontSize="36" FontWeight="Bold" />
            <TextBlock Text="weekly total" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />

            <TextBlock Text="{Binding TopCategory}" FontSize="36" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
            <TextBlock Text="top category" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,-2,0,18" />
            
        </StackPanel>
    </ScrollViewer>
</UserControl>
