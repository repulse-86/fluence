<Page
    x:Class="Fluence.Views.QuickAddPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="0,2,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding HeaderTitle}" Style="{ThemeResource HeaderTextBlockStyle}" Margin="19,0,-100,26" TextWrapping="NoWrap" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="19,0,19,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="category" Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,5" IsHitTestVisible="False" />
                <ComboBox Grid.Row="1" x:Name="CategoryComboBox"
                          ItemsSource="{Binding Categories}" 
                          DisplayMemberPath="Name" 
                          SelectedValuePath="Id" 
                          SelectedValue="{Binding CategoryId, Mode=TwoWay}"
                          HorizontalAlignment="Stretch" />

                <TextBlock Grid.Row="2" Text="amount" Style="{StaticResource BodyTextBlockStyle}" Margin="0,20,0,5" IsHitTestVisible="False" />
                <TextBox Grid.Row="3" x:Name="AmountTextBox" Text="{Binding AmountText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Number" KeyDown="AmountTextBox_KeyDown" TextChanged="AmountTextBox_TextChanged" />
                
                <TextBlock Grid.Row="4" Text="note" Style="{StaticResource BodyTextBlockStyle}" Margin="0,20,0,5" IsHitTestVisible="False" />
                <TextBox Grid.Row="5" x:Name="NoteTextBox"
                         Text="{Binding Note, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         Height="120" 
                         VerticalAlignment="Top" />

                <TextBlock Grid.Row="6" Text="type" Style="{StaticResource BodyTextBlockStyle}" Margin="0,20,0,5" IsHitTestVisible="False" />
                <Grid Grid.Row="7">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <RadioButton Grid.Column="0" Content="Expense" IsChecked="{Binding Type, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Expense, Mode=TwoWay}" GroupName="Type" />
                    <RadioButton Grid.Column="1" Content="Income" IsChecked="{Binding Type, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Income, Mode=TwoWay}" GroupName="Type" Margin="20,0,0,0" />
                </Grid>

                <TextBlock Grid.Row="8" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,20,0,0" TextWrapping="Wrap" IsHitTestVisible="False" />
                <TextBlock Grid.Row="9" Text="{Binding DebugData}" Foreground="{Binding DebugColor}" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center" FontWeight="SemiBold" IsHitTestVisible="False" />
            </Grid>
        </ScrollViewer>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="SaveTransactionAppBarButton" Icon="Save" Label="save" Click="SaveTransactionAppBarButton_Click" />
            <AppBarButton x:Name="CancelAppBarButton" Icon="Cancel" Label="cancel" Click="CancelAppBarButton_Click" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
