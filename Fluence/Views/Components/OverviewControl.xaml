<UserControl
    x:Class="Fluence.Views.Components.OverviewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components"
    xmlns:converters="using:Fluence.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="360">

    <UserControl.Resources>
        <converters:WidthConverter x:Key="WidthConverter" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,16">
            <TextBlock Text="{Binding CurrentBalance, ConverterParameter='N2'}" FontSize="42" FontWeight="Bold" />
            <TextBlock Text="current balance" FontSize="12" Opacity="0.6" Margin="0,-8,0,20" />

            <Border Height="30" Background="#1AFFFFFF" Margin="0,0,0,5">
                <Grid>
                    <Rectangle Fill="{Binding BudgetBrush}" HorizontalAlignment="Left">
                        <Rectangle.Width>
                            <Binding Path="BudgetPercent">
                                <Binding.Converter>
                                    <StaticResource ResourceKey="WidthConverter" />
                                </Binding.Converter>
                                <Binding.ConverterParameter>340</Binding.ConverterParameter>
                            </Binding>
                        </Rectangle.Width>
                    </Rectangle>
                </Grid>
            </Border>
            <TextBlock Text="monthly budget health" FontSize="12" Opacity="0.6" Margin="0,0,0,30" />

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="150" />
                    <RowDefinition Height="150" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Background="#33FFFFFF" Margin="0,0,5,5" Padding="15">
                    <Grid>
                        <TextBlock Text="daily allowance" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Top" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding DailyAllowance, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
                    </Grid>
                </Border>

                <Border Grid.Row="0" Grid.Column="1" Background="#33FFFFFF" Margin="5,0,0,5" Padding="15">
                    <Grid>
                        <TextBlock Text="spent today" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Top" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding SpentToday, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
                    </Grid>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Background="#33FFFFFF" Margin="0,5,5,0" Padding="15">
                    <Grid>
                        <TextBlock Text="weekly total" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Top" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding WeeklyTotal, ConverterParameter='N2'}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
                    </Grid>
                </Border>

                <Border Grid.Row="1" Grid.Column="1" Background="#33FFFFFF" Margin="5,5,0,0" Padding="15">
                    <Grid>
                        <TextBlock Text="top category" FontSize="14" VerticalAlignment="Top" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding TopCategory}" FontSize="24" FontWeight="Bold" VerticalAlignment="Bottom" HorizontalAlignment="Right" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
