<Page
    x:Class="Fluence.Views.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views"
    xmlns:components="using:Fluence.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="REPORTS"
                   Style="{ThemeResource BodyTextBlockStyle}"
                   Margin="19,10,0,0"
                   FontSize="16" />

        <Pivot x:Name="ReportsPivot" Grid.Row="1" Margin="0,-36,0,0">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="56" FontWeight="SemiLight" />
                </DataTemplate>
            </Pivot.HeaderTemplate>

            <PivotItem Header="today" DataContext="{Binding DayReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,20,19,30">
                        <TextBlock Text="spending" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <components:ReportDistributionControl DataContext="{Binding Distribution}" />
                        <components:ReportBurnersControl DataContext="{Binding BudgetBurners}" />
                        <components:ReportBreakdownControl DataContext="{Binding Distribution}" />

                        <Rectangle Height="1" Fill="Gray" Margin="0,0,0,40" />

                        <TextBlock Text="flow" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <components:ReportComparisonControl 
 TrendText="vs yesterday" ShowProjections="False" />
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="month" DataContext="{Binding MonthReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,20,19,30">
                        <TextBlock Text="activity" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <TextBlock Text="weekday" FontSize="16" Margin="0,0,0,5" Foreground="White" />
                        <components:ReportWeekdayControl DataContext="{Binding WeekdaySpending}" />
                        <TextBlock Text="intensity" FontSize="16" Margin="0,15,0,5" Foreground="White" />
                        <components:ReportIntensityControl DataContext="{Binding IntensityMap}" IsYearly="False" />

                        <Rectangle Height="1" Fill="Gray" Margin="0,0,0,40" />

                        <TextBlock Text="spending" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <components:ReportDistributionControl DataContext="{Binding Distribution}" />
                        <components:ReportBurnersControl DataContext="{Binding BudgetBurners}" />
                        <components:ReportBreakdownControl DataContext="{Binding Distribution}" />

                        <Rectangle Height="1" Fill="Gray" Margin="0,0,0,40" />

                        <TextBlock Text="flow" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <components:ReportComparisonControl 
 TrendText="vs last month" ShowProjections="True" AverageLabel="daily average" ProjectedLabel="projected total" />
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="year" DataContext="{Binding YearReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,20,19,30">
                        <TextBlock Text="activity" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <TextBlock Text="monthly intensity" FontSize="16" Margin="0,0,0,5" Foreground="White" />
                        <components:ReportIntensityControl DataContext="{Binding IntensityMap}" IsYearly="True" />

                        <Rectangle Height="1" Fill="Gray" Margin="0,0,0,40" />

                        <TextBlock Text="spending" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <components:ReportDistributionControl DataContext="{Binding Distribution}" />
                        <components:ReportBurnersControl DataContext="{Binding BudgetBurners}" />
                        <components:ReportBreakdownControl DataContext="{Binding Distribution}" />

                        <Rectangle Height="1" Fill="Gray" Margin="0,0,0,40" />

                        <TextBlock Text="flow" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="24" Margin="0,0,0,10" />
                        <components:ReportComparisonControl 
 TrendText="vs last year" ShowProjections="True" AverageLabel="monthly average" ProjectedLabel="projected year" />
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="insights" DataContext="{Binding MonthReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,20,19,30">
                        <components:ReportRunwayControl />

                        <Rectangle Height="1" Fill="Gray" Margin="0,0,0,40" />

                        <components:ReportOpportunitiesControl DataContext="{Binding SavingOpportunities}" />

                        <Rectangle Height="1" Fill="Gray" Margin="0,0,0,40" />

                        <components:ReportSpendingPatternsControl />
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
