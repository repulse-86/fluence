<UserControl
    x:Class="Fluence.Views.Components.ReportsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components"
    xmlns:converters="using:Fluence.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="360">

    <Grid Margin="-16,-44,0,0">
        <Pivot x:Name="ReportsPivot">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="24" FontWeight="SemiBold" />
                </DataTemplate>
            </Pivot.HeaderTemplate>

            <!-- DAY REPORT -->
            <PivotItem Header="day" DataContext="{Binding DayReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <StackPanel>
                        <TextBlock Text="distribution" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportDistributionControl DataContext="{Binding Distribution}" />

                        <TextBlock Text="comparison" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportComparisonControl TrendText="vs yesterday" />

                        <TextBlock Text="top budget burners" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportBurnersControl DataContext="{Binding BudgetBurners}" />

                        <TextBlock Text="breakdown" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportBreakdownControl DataContext="{Binding Distribution}" />
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <!-- MONTH REPORT -->
            <PivotItem Header="month" DataContext="{Binding MonthReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="distribution" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportDistributionControl DataContext="{Binding Distribution}" />

                        <TextBlock Text="spending intensity" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportIntensityControl DataContext="{Binding IntensityMap}" IsYearly="False" />

                        <TextBlock Text="comparison" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportComparisonControl TrendText="vs last month" />

                        <Grid Margin="0,0,0,30">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock Text="daily average" FontSize="12" />
                                <TextBlock Text="{Binding DailyAverage, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock Text="projected total" FontSize="12" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding ProjectedTotal, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Right" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="top budget burners" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportBurnersControl DataContext="{Binding BudgetBurners}" />

                        <TextBlock Text="breakdown" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportBreakdownControl DataContext="{Binding Distribution}" />
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>

            <!-- YEAR REPORT -->
            <PivotItem Header="year" DataContext="{Binding YearReport}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" Margin="-4">
                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="distribution" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportDistributionControl DataContext="{Binding Distribution}" />

                        <TextBlock Text="monthly intensity" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportIntensityControl DataContext="{Binding IntensityMap}" IsYearly="True" />

                        <TextBlock Text="comparison" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportComparisonControl TrendText="vs last year" />

                        <Grid Margin="0,0,0,30">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock Text="monthly average" FontSize="12" />
                                <TextBlock Text="{Binding DailyAverage, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock Text="projected year" FontSize="12" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding ProjectedTotal, ConverterParameter='N2'}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Right" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="top budget burners" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportBurnersControl DataContext="{Binding BudgetBurners}" />

                        <TextBlock Text="breakdown" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="18" Margin="0,0,0,10" />
                        <local:ReportBreakdownControl DataContext="{Binding Distribution}" />
                    </StackPanel>
                </ScrollViewer>
            </PivotItem>
        </Pivot>
    </Grid>
</UserControl>
