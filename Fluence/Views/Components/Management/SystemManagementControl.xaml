<UserControl
    x:Class="Fluence.Views.Components.Management.SystemManagementControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluence.Views.Components.Management"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="400">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,20,0,0">
            <TextBlock Text="background" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,0,0,15" IsHitTestVisible="False" />

            <StackPanel Margin="0,0,0,25">
                <TextBlock Text="auto refresh wallpaper" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" IsHitTestVisible="False" />
                <TextBlock Text="periodically refresh the background wallpaper" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,10" IsHitTestVisible="False" />
                <ToggleSwitch IsOn="{Binding IsIntervalRefreshEnabled, Mode=TwoWay}" Toggled="AutoRefreshWallpaper_Toggled" />
            </StackPanel>

            <StackPanel Margin="0,0,0,25" Visibility="{Binding IntervalInputVisibility}">
                <TextBlock Text="refresh interval" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" IsHitTestVisible="False" />
                <TextBlock Text="time between wallpaper changes" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,10" IsHitTestVisible="False" />
                <ComboBox SelectedIndex="0" SelectionChanged="IntervalComboBox_SelectionChanged">
                    <ComboBoxItem Content="every 15 minutes" Tag="15" />
                    <ComboBoxItem Content="every 30 minutes" Tag="30" />
                    <ComboBoxItem Content="every 45 minutes" Tag="45" />
                    <ComboBoxItem Content="every 60 minutes" Tag="60" />
                </ComboBox>
            </StackPanel>

            <TextBlock Text="data" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}" Foreground="{ThemeResource SystemColorControlAccentBrush}" FontSize="20" Margin="0,0,0,15" IsHitTestVisible="False" />

            <StackPanel Margin="0,0,0,25">
                <TextBlock Text="mock data" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" IsHitTestVisible="False" />
                <TextBlock Text="this will delete all existing data and generate 30 days of realistic mock transactions to test the reports." Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,10" IsHitTestVisible="False" />
                <Button x:Name="MockDataButton" Content="activate mock data" HorizontalAlignment="Stretch" Click="MockData_Click" />
            </StackPanel>

            <StackPanel Margin="0,0,0,25">
                <TextBlock Text="clear all data" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" IsHitTestVisible="False" />
                <TextBlock Text="danger: this permanently deletes all transactions. this action cannot be undone." Style="{StaticResource BodyTextBlockStyle}" Opacity="0.9" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,10" IsHitTestVisible="False" />
                <Button x:Name="ClearDataButton" Content="clear all data" HorizontalAlignment="Stretch" Background="#E74C3C" Foreground="White" Click="ClearData_Click" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
